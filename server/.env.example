# PostgreSQL Database Configuration
DATABASE_URL=postgresql://user:password@host:port/dbname

# Server Configuration
PORT=

# JWT Secret (generate a strong random string)
JWT_SECRET=

# Resend Email Configuration (Replaces Gmail SMTP)
RESEND_API_KEY=
